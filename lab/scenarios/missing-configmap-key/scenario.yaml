title: "shop-api pods fail to start: a required ConfigMap key is missing"
scope:
  namespace: shop
  app: shop-api
task_prompt: "shop-api pods have been stuck for a while and never go Ready"
expected_outcome: pr
symptom_probe:
  kind: pod_waiting_reason
  value: CreateContainerConfigError
  timeout_s: 60
tags: [config, startup]
