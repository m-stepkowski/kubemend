apiVersion: v2
name: kubemend
description: GitOps-native Kubernetes remediation agent that can only open pull requests
type: application
version: 0.1.0
# Bumped alongside project tags — a manual sync point, same drift risk noted
# for the pinned tool binaries in Dockerfile/Taskfile.yaml.
appVersion: "0.8"
home: https://github.com/m-stepkowski/kubemend
